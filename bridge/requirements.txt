# Depth bridge runtime dependencies. Install with: pip install -r bridge/requirements.txt
numpy>=1.24
websockets>=12

# Optional: true multi-blob separation (connected components). Without it every
# in-range pixel is reported as one blob. Required for the Leap Motion Controller
# stereo source (--source leap / leap-synthetic, see leap_stereo.py).
# opencv-python>=4.8

# Optional: Intel RealSense cameras (--source realsense).
# pyrealsense2>=2.54
